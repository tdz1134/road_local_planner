<?xml version="1.0"?>
<!--
  loop_goal.launch：有定位 + 终点导航（follow_road=false），世界为闭合环形道路

  世界：loop_road.world —— 首尾闭合的纯道路（直道 + 小曲率缓弯 + 适中 U 形弯），
        路宽 6m、无障碍。由 scripts/gen_road_world.py 参数化生成。

  一键启动：Gazebo(loop_road) + Scout v2 + 定位 + 栅格 + 导航(终点模式) + RViz

  与 loop_road.launch（无定位沿路）的区别：
    · 始终启动 localization_node：Gazebo 真值 → /odom + TF(odom→base_link)。
    · 导航配置：nav_params.yaml（follow_road=false）—— NavCore 把 RViz 里点的全局
      终点经定位投影成车体系子目标，走 A*（扇形展开绕障）+ 纯跟踪。
    · 车不会自动沿路绕圈：需要**手动沿路点一连串 goal**才能贴着道路走；否则它会
      凭 A* 直穿可见范围内的自由区（环内草地激光打不到→乐观可行，可能斜穿）。
      想在道路上做端到端演示，请沿中心线多次点 2D Nav Goal。
    · RViz Fixed Frame=odom（有定位，全局路径在 odom 系）。

  用法：
    roslaunch unk_nav_sim loop_goal.launch
    roslaunch unk_nav_sim loop_goal.launch gui:=false      # 无头模式
    在 RViz 工具栏点 "2D Nav Goal" 设定终点。
-->
<launch>

  <arg name="gui" default="true"/>

  <!-- 出生点：第一段直道上，车头朝 +X。与 world 的 SPAWN 一致 -->
  <arg name="x"   default="2.0"/>
  <arg name="y"   default="0.0"/>
  <arg name="yaw" default="0.0"/>

  <!-- ══════════════════════════════════════════════════════════ -->
  <!-- 1. Gazebo + loop_road（闭合环形道路）                        -->
  <!-- ══════════════════════════════════════════════════════════ -->
  <include file="$(find gazebo_ros)/launch/empty_world.launch">
    <arg name="world_name"   value="$(find unk_nav_sim)/worlds/loop_road.world"/>
    <arg name="paused"       value="false"/>
    <arg name="use_sim_time" value="true"/>
    <arg name="gui"          value="$(arg gui)"/>
    <arg name="headless"     value="false"/>
    <arg name="debug"        value="false"/>
  </include>

  <!-- ══════════════════════════════════════════════════════════ -->
  <!-- 2. Scout v2（含 Velodyne HDL-32E + robot_state_publisher）  -->
  <!-- ══════════════════════════════════════════════════════════ -->
  <include file="$(find scout_gazebo_sim)/launch/spawn_scout_v2.launch">
    <arg name="x"   value="$(arg x)"/>
    <arg name="y"   value="$(arg y)"/>
    <arg name="z"   value="0.0"/>
    <arg name="yaw" value="$(arg yaw)"/>
  </include>

  <!-- ══════════════════════════════════════════════════════════ -->
  <!-- 3. 定位节点（Gazebo 真值 → /odom + TF）                    -->
  <!-- ══════════════════════════════════════════════════════════ -->
  <node name="localization_node" pkg="unk_nav_sim" type="localization_node"
        output="screen">
    <param name="model_name" value="scout/"/>
  </node>

  <!-- ══════════════════════════════════════════════════════════ -->
  <!-- 4. 栅格节点（/velodyne_points → /local_grid）              -->
  <!-- ══════════════════════════════════════════════════════════ -->
  <node name="grid_node" pkg="unk_nav_sim" type="grid_node" output="screen">
    <rosparam file="$(find unk_nav_sim)/config/grid_params.yaml" command="load"/>
  </node>

  <!-- ══════════════════════════════════════════════════════════ -->
  <!-- 5. 导航节点（终点模式：nav_params.yaml，follow_road=false）  -->
  <!-- ══════════════════════════════════════════════════════════ -->
  <node name="nav_node" pkg="unk_nav_sim" type="nav_node" output="screen">
    <param name="config_file" value="$(find unk_nav)/config/nav_params.yaml"/>
  </node>

  <!-- ══════════════════════════════════════════════════════════ -->
  <!-- 6. RViz（Fixed Frame=odom，全局终点导航）                   -->
  <!-- ══════════════════════════════════════════════════════════ -->
  <node name="rviz" pkg="rviz" type="rviz"
        args="-d $(find unk_nav_sim)/config/unk_nav.rviz"
        required="false"/>

</launch>
