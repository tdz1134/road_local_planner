快速启动

每个新终端先 source：

```bash
source ~/projects/road_local_planner/devel/setup.bash
cd ~/projects/road_local_planner
```

改过 C++ 或 nav_params*.yaml 才需要编译，否则 Gazebo 跑的还是旧二进制：

```bash
catkin_make -j4
```

带障碍（新增，Gazebo 里红色柱子、橙色块）

```bash
roslaunch unk_nav_sim obstacle_tight_road.launch   # 路宽 3.6m，s=58.5 全封死：预期停住 → RECOVERY → ABORT
roslaunch unk_nav_sim obstacle_mid_road.launch     # 路宽 6m，每个断面都留口：预期逐个绕过后跑完一圈
roslaunch unk_nav_sim obstacle_tight_goal.launch   # 终点模式，要在 RViz 点 2D Nav Goal 才动
roslaunch unk_nav_sim obstacle_mid_goal.launch
```

纯道路，无障碍，作对照

```bash
roslaunch unk_nav_sim loop_road.launch             # 路宽 6m 闭合环，沿路模式自己跑
roslaunch unk_nav_sim loop_goal.launch             # 同一地图，终点模式
roslaunch unk_nav_sim loop_extreme_road.launch     # 路宽 3m + S 弯 + 180° 发夹
roslaunch unk_nav_sim road_follow.launch           # 直走廊 + 两个路中圆柱（老场景）
```

开阔地

```bash
roslaunch unk_nav_sim open_goal.launch             # 200×200 大世界，出生点 (-95,-95)
roslaunch unk_nav_sim walls_goal.launch
```

可加参数（所有 launch 通用）

```bash
gui:=false                     # 无头，快很多
x:=3.0 y:=1.0 yaw:=1.57        # 覆盖出生点
use_localization:=true         # 只有 *_road 有这个参数，默认 false
```

改地图形状就改脚本，不要手改 world

```bash
python3 scripts/gen_obstacle_world.py                     # 重新生成 obstacle_*.world 和 4 个 launch
python3 scripts/gen_obstacle_world.py -p tight --ascii    # 只看俯视示意图和断面净宽，不写文件
python3 scripts/gen_road_world.py                         # 纯道路版
```

沿路模式（*_road）不需要点目标；终点模式（*_goal）要沿路点一连串 goal，否则它会斜穿环内草地（那块激光打不到，栅格里是自由的）。
