快速启动

每个新终端先 source：

```bash
source ~/projects/road_local_planner/devel/setup.bash
cd ~/projects/road_local_planner
```

改过 C++ 或 nav_params*.yaml 才需要编译，否则 Gazebo 跑的还是旧二进制：

```bash
catkin_make -j4
```

改 scripts/ 里的生成脚本、.world、.launch、README 不用编译：devel 工作区下
`$(find unk_nav_sim)` 直接解析到 `src/unk_nav_sim/`，重跑脚本后 roslaunch 立刻读到新文件。
（只有用 `catkin_make install` 的 install 空间才要重装，那才会走 `install(DIRECTORY worlds launch config)`。）

---

## 沿路模式（follow_road=true，无需定位）

所有 `*_road` 和 `*_loop` / `test_*` / `img_*` 场景都是沿路模式，不需要在 RViz 点目标。

### 带障碍（Gazebo 里圆柱红、方块橙，墙和封锁块也是橙色）

```bash
# 脚本 gen_obstacle_world.py 生成
roslaunch unk_nav_sim obstacle_tight_road.launch     # 路宽 3.6m，s=58.5 全封死：预期停住 → RECOVERY → ABORT
roslaunch unk_nav_sim obstacle_mid_road.launch       # 路宽 6m，每个断面都留口：预期逐个绕过后跑完一圈
roslaunch unk_nav_sim obstacle_taper_road.launch     # 路宽 3.2~7m 渐变 + 障碍：同一根柱在宽处松、窄处封
roslaunch unk_nav_sim obstacle_asymturn_road.launch  # 圆弧弯+直角弯，直角前单侧内缩墙吃掉左半 1.2m
```

### 纯道路，无障碍，作对照

```bash
# 脚本 gen_road_world.py 生成
roslaunch unk_nav_sim loop_road.launch               # 路宽 6m 闭合环
roslaunch unk_nav_sim loop_stadium_road.launch       # 5m 体育场环：几何最简，改参数后先跑它定基线
roslaunch unk_nav_sim loop_taper_road.launch         # 3.2~7m 变路宽闭合环
roslaunch unk_nav_sim loop_asymturn_road.launch      # 一侧大圆弧(右缘 R7.2) + 一侧近直角(左缘 R1.2)
roslaunch unk_nav_sim loop_extreme_road.launch       # 路宽 3m + S 弯 + 180° 发夹
```

### 图片生成场景（gen_road_from_image.py）

```bash
roslaunch unk_nav_sim ellipse_loop.launch            # 椭圆 loop（变宽 10~20m）
roslaunch unk_nav_sim img_ellipse_obs.launch         # 椭圆 loop + 4 个障碍
roslaunch unk_nav_sim img_rect_obs.launch            # 矩形 loop + 7 个障碍
roslaunch unk_nav_sim test_figure8.launch            # 8 字形 loop
roslaunch unk_nav_sim test_l_shape.launch            # L 形 loop（直角转弯）
roslaunch unk_nav_sim test_variable_width.launch     # 矩形 loop（左宽右窄）
```

### 手写经典场景

```bash
roslaunch unk_nav_sim road_follow.launch             # 直走廊 + 两个路中圆柱（老场景）
```

---

## 终点模式（follow_road=false，需要定位 + 终点）

`*_goal` 场景和以下手写场景是终点模式。沿路版的 `*_road` 加 `use_localization:=true`
再在 RViz 点 2D Nav Goal 也能切到终点模式（但 yaml 里 follow_road 仍是 true，需手动改）。

### 带障碍

```bash
roslaunch unk_nav_sim obstacle_tight_goal.launch     # 同 tight 地图，终点模式
roslaunch unk_nav_sim obstacle_mid_goal.launch       # 同 mid 地图，终点模式
roslaunch unk_nav_sim obstacle_taper_goal.launch     # 同 taper 地图，终点模式
roslaunch unk_nav_sim obstacle_asymturn_goal.launch  # 同 asymturn 地图，终点模式
```

### 纯道路

```bash
roslaunch unk_nav_sim loop_goal.launch               # 同 loop 地图
roslaunch unk_nav_sim loop_stadium_goal.launch       # 同 stadium 地图
roslaunch unk_nav_sim loop_taper_goal.launch         # 同 taper 地图
roslaunch unk_nav_sim loop_asymturn_goal.launch      # 同 asymturn 地图
roslaunch unk_nav_sim loop_extreme_goal.launch       # 同 extreme 地图
```

### 开阔地 / 走廊

```bash
roslaunch unk_nav_sim open_goal.launch               # 200×200 大世界，出生点 (-95,-95)
roslaunch unk_nav_sim walls_goal.launch              # 墙壁迷宫
roslaunch unk_nav_sim road_nav.launch                # 直走廊 + 圆柱（与 road_follow 同地图，终点模式）
```

---

## 通用 launch 参数（所有场景通用）

```bash
gui:=false                     # 无头，快很多
x:=3.0 y:=1.0 yaw:=1.57        # 覆盖出生点
use_localization:=true         # 仅 *_road / *_loop / test_* / img_* / road_follow 有，默认 false
```

终点模式（`*_goal` / `road_nav` / `open_goal` / `walls_goal`）本来就必开定位，
没有 `use_localization` 参数。

终点模式要在 RViz 里点 **2D Nav Goal** 才动车；沿路模式启动即跑。
沿路模式的 `*_goal` 版需沿路点一连串 goal，否则它会斜穿环内草地（那块激光打不到，栅格里是自由的）。

---

## 地图生成脚本

改地图形状就改脚本，不要手改 world：

```bash
python3 scripts/gen_obstacle_world.py                     # 重新生成 4 个 obstacle_*.world + 配套 launch
python3 scripts/gen_obstacle_world.py -p tight --ascii    # 只看俯视示意图和断面净宽，不写文件
python3 scripts/gen_road_world.py                         # 纯道路版（5 个 loop_*.world）
python3 scripts/gen_road_world.py -p asymturn --ascii     # 单个场景的六项校验 + 段表 + 两侧路缘半径
python3 scripts/gen_road_from_image.py                    # 从图片生成道路 world（ellipse/img_*/test_*）
```

脚本出的 launch 头部带 `AUTO-GENERATED by` 标记，只有带标记的才会被覆盖；手写的
`loop_road.launch` / `loop_extreme_*.launch`（里有实测告警）会被跳过并打印提示。

---

## 新场景验证状态

`loop_stadium` / `loop_taper` / `loop_asymturn` / `obstacle_taper` / `obstacle_asymturn` 只过了
静态检查（六项几何校验、断面净宽推导、XML well-formed、launch↔world 出生点比对、二次生成幂等），
**还没在 Gazebo 里实跑过**。

建议验证顺序：`loop_stadium_road` → `loop_asymturn_road` →
`obstacle_taper_road` → `obstacle_asymturn_road`。第一步几何最简，它都不稳就说明是实现退化、
不是场景难。
